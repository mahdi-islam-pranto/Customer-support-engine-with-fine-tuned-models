streamlit==1.37.0

# ML / inference
torch==2.3.1
transformers==4.44.0
peft==0.12.0
accelerate==0.33.0
sentence-transformers==3.0.1

# bitsandbytes — required for 4-bit quant on GPU.
# On CPU-only Docker builds this still installs fine but 4-bit is skipped.
bitsandbytes==0.43.3

# FAISS — CPU version (swap for faiss-gpu if your host has a GPU)
faiss-cpu==1.8.0

# HuggingFace Hub
huggingface_hub==0.24.5